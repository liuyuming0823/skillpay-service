# ============================================================
# SkillPay.Service
# 关键：任何含密钥的配置都不得进版本库
# ============================================================

# --- 密钥与本地配置（最高优先级）---
appsettings.Development.json
appsettings.Production.json
appsettings.*.Local.json
# 回归验证脚本（ym-skillpay-server-verify）临时生成，跑完会自动删；忽略以防误提交
appsettings.Verify.json

# Python 编译缓存（pack-dist.py / pack-skill.py 运行后产生）
__pycache__/
*.pyc
*.pem
*.key
*.p12
*.pfx
secrets.json

# --- 运行期数据（SQLite 订单库）---
# 注意：这里绝对不能写 `data/`。Windows 上 git 不区分大小写，`data/` 会连带
# 忽略 C# 源码目录 `Data/`，导致数据层代码进不了仓库、服务器编译直接失败。
# 因此只按扩展名忽略，并把运行期目录改名为 appdata/ 与源码目录错开。
appdata/
*.db
*.db-shm
*.db-wal

# --- .NET 构建产物 ---
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
.vs/

# --- 测试与覆盖率 ---
[Tt]est[Rr]esult*/
coverage*.json
coverage*.xml
*.trx
*.coverage

# --- IDE / 编辑器 ---
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# --- 日志与临时文件 ---
*.log
logs/
tmp/
*.tmp

# --- 交付包产物（scripts/pack-dist.py 的输出目录）---
# 二进制包每次打包都会变，进版本库只会让仓库持续膨胀。
# 版本追溯靠三样东西：包名里的版本号、包 sha256、部署时写入配置的 PayloadVersion。
# 注：dist/ 下 v1.0.0 那个包在加这条规则之前就已被跟踪，保持原状不动。
dist/
